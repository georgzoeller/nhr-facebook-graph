// Generated by CoffeeScript 2.3.2
(function() {
  module.exports = function(context) {
    return {
      addGroup: function(community_id, {name, description = '', privacy = 'OPEN'}) {
        return context.edge(`${community_id}`, 'groups').describe(`Creating group(${name}) on ${community_id} with privacy ${privacy}`).authorizeByToken().setMethod('POST').setJson({
          name: name,
          privacy: privacy,
          description: description
        });
      },
      getAppCommunity: function() {
        return context.node('community').describe('Retrieving community information').authorizeByToken().setMethod('GET');
      },
      getGroups: function(community_id) {
        return context.edge(community_id, 'groups').describe('getting groups').authorizeByToken().setMethod('GET').allPages();
      }
    };
  };

}).call(this);
